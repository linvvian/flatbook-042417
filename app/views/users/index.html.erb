<div class="well" style="padding: 30px">
  <h1>Results</h1>
  <br>
  <div class="list-group">
    <% if @users.present? %>
      <%= render 'users' %>
    <% elsif params[:search] %>
      <h4>There are no users containing [ <%= params[:search] %> ].</h4>
    <% end %>
  </div>
</div>
