<div class="list-group">
  <% @users.each do |user| %>
    <p><h4><%= link_to "#{image_tag(user.image, size:"40x40")} #{user.name}".html_safe, user_path(user), class: "list-group-item" %></h4></p>
  <% end %>
</div>
