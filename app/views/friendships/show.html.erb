<h1>Your Friends</h1>

<div class="well">
  <div class="list-group">
    <% for user in @friends %>
      <%= link_to user.name, user_path(user), class: "list-group-item" %>
    <% end %>
  </div>
</div>
