added a comment to the page

<% if activity.trackable %>
  <% @type = activity.trackable.event || activity.trackable.group || activity.trackable.user || activity.trackable.project %>
  <%= link_to @type.name, activity.trackable.event || activity.trackable.group || activity.trackable.user || activity.trackable.project %>
<% else %>
  which has been removed
<% end %>
